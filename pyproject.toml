[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gemini"
version = "0.1.0"
description = "Reusable Gemini API client helper"
authors = [{ name = "Daniel" }]
dependencies = ["google-genai"]
